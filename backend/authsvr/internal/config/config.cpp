#include "config.h"

namespace swift::auth {

AuthConfig LoadConfig(const std::string& config_file) {
    AuthConfig config;
    // TODO: 从文件或环境变量加载配置
    return config;
}

}  // namespace swift::auth
