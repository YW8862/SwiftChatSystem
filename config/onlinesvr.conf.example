# OnlineSvr 配置示例
# 复制为 onlinesvr.conf 后按需修改；环境变量 ONLINESVR_* 可覆盖

host=0.0.0.0
port=9095

store_type=rocksdb
rocksdb_path=/data/online

# JWT 签发与校验（Login/Logout/ValidateToken）；生产环境建议用环境变量
jwt_secret=swift_online_secret_2026
jwt_expire_hours=168

log_dir=/data/logs
log_level=INFO
