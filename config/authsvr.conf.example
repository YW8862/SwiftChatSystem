# AuthSvr 配置示例
# 复制为 authsvr.conf 后按需修改；环境变量 AUTHSVR_* 可覆盖同名项（如 AUTHSVR_PORT=9094）

# 服务
host=0.0.0.0
port=9094

# 存储
store_type=rocksdb
rocksdb_path=/data/auth
# mysql_dsn=user:pass@tcp(localhost:3306)/auth

# JWT 校验密钥（与 OnlineSvr 一致，GetProfile/UpdateProfile 鉴权用）；生产环境建议用环境变量 AUTHSVR_JWT_SECRET
jwt_secret=swift_online_secret_2026

# 日志
log_dir=/data/logs
log_level=INFO
