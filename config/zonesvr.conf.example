# ZoneSvr 配置示例
# 复制为 zonesvr.conf 后按需修改；环境变量 ZONESVR_* 可覆盖

host=0.0.0.0
port=9092

# 会话存储：memory / redis
session_store_type=memory
redis_url=redis://localhost:6379

session_expire_seconds=3600
gate_heartbeat_timeout=30

log_dir=/data/logs
log_level=INFO
