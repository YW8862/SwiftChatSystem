# GateSvr 配置示例
# 复制为 gatesvr.conf 后按需修改；环境变量 GATESVR_* 可覆盖

host=0.0.0.0
websocket_port=9090
grpc_port=9091

# ZoneSvr 地址（供本机转发请求）
zone_svr_addr=localhost:9092

max_connections=10000
heartbeat_interval_seconds=30
heartbeat_timeout_seconds=90

log_dir=/data/logs
log_level=INFO
