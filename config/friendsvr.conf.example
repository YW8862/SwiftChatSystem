# FriendSvr 配置示例
# 复制为 friendsvr.conf 后按需修改；环境变量 FRIENDSVR_* 可覆盖

host=0.0.0.0
port=9096

store_type=rocksdb
rocksdb_path=/data/friend
# mysql_dsn=user:pass@tcp(localhost:3306)/friend

# 与 OnlineSvr 一致的 JWT 密钥（鉴权用）；生产环境建议用环境变量 FRIENDSVR_JWT_SECRET
jwt_secret=swift_online_secret_2026

log_dir=/data/logs
log_level=INFO
