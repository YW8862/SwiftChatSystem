apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: master

resources:
  - namespace.yaml
  - configmap.yaml
  - persistent-volume.yaml
  - redis-deployment.yaml
  - gatesvr-deployment.yaml
  - zonesvr-deployment.yaml
  - authsvr-deployment.yaml
  - onlinesvr-deployment.yaml
  - friendsvr-deployment.yaml
  - chatsvr-deployment.yaml
  - filesvr-deployment.yaml

# 保留 commonLabels：labels 会修改 selector 导致与已有 Deployment 冲突（field is immutable）
commonLabels:
  app.kubernetes.io/name: master
  app.kubernetes.io/version: "0.1.0"
