# 使用 build 目录的 compile_commands.json（与真实编译一致，含 proto 生成头路径）
CompileFlags:
  CompilationDatabase: build
  Add:
    - -I/usr/include
    - -I/usr/include/rocksdb
    - -I/usr/include/nlohmann
    # proto 生成头（如 auth.grpc.pb.h）所在目录，build 一次后存在；无 build 时编辑器可能仍报错
    - -Ibuild/backend/common/proto/generated
  Remove:
    - -W*

Diagnostics:
  UnusedIncludes: None
